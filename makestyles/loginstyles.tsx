import { makeStyles } from '@material-ui/core';

export const loginsty = makeStyles((theme) => ({
  Cbox: {
    width: '100%',
    height: '100vh',
    display: 'flex',
    justifyContent: 'center',
    alignItems: 'center',
  },

  Mbox: {
    display: 'flex',
    justifyContent: 'center',
    alignItems: 'center',
  },
}));
